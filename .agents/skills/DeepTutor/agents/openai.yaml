interface:
  display_name: "DeepTutor"
  short_description: "Repo-specific patterns and workflows for DeepTutor"
  default_prompt: "Use the DeepTutor repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true